---
phase: 10-migration-launch
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/payload-import.ts
  - scripts/media-uploader.ts
autonomous: false

must_haves:
  truths:
    - "All WordPress content imported to staging environment"
    - "Artist reviews and approves their migrated content"
    - "Row counts verified to match expected counts exactly"
  artifacts:
    - path: "scripts/payload-import.ts"
      provides: "Payload CMS import script"
    - path: "scripts/media-uploader.ts"
      provides: "Media migration to MinIO"
  key_links:
    - from: "scripts/payload-import.ts"
      to: "Payload CMS API"
      via: "Local API"
      pattern: "payload.create"
---

<objective>
Import transformed data to staging Payload CMS and obtain artist approval with verified row counts.

Purpose: Ensure migrated content is complete AND correct before production cutover.
Output: Artist-approved staging site with verified data counts matching source.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-migration-launch/10-01-SUMMARY.md
@data/expected-counts.json
@data/transformed-data.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import Data to Staging Payload CMS</name>
  <files>scripts/payload-import.ts, scripts/media-uploader.ts</files>
  <action>
    Create Payload import script that:
    1. Reads transformed-data.json
    2. Creates TeamMember records for each artist
    3. Creates Portfolio records with relationships to TeamMembers
    4. Creates PortfolioMedia records linked to Portfolio items
    5. Logs progress: "Importing artist 3/5: Name..."
    6. Handles errors gracefully - log but continue (report failures at end)

    Create media uploader that:
    1. Downloads images from WordPress URLs
    2. Processes through Sharp.js pipeline (WebP, focal points)
    3. Uploads to MinIO staging bucket
    4. Updates Payload Media records with S3 URLs

    After import, log actual counts:
    ```
    Import complete:
    - Artists imported: X
    - Portfolio items imported: X
    - Media files uploaded: X
    - Errors: X (see error-log.json)
    ```
  </action>
  <verify>
    - `npx ts-node scripts/payload-import.ts` completes
    - No critical errors in output (warnings OK)
    - Staging site shows imported content at https://staging-kinelab.fodivps2.cloud
  </verify>
  <done>All transformed data imported to staging environment</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Artist Review and Data Count Verification (CRITICAL)</name>
  <what-built>
    Complete data migration from WordPress to Payload CMS staging environment.
    All artists, portfolio items, and media should be visible.
  </what-built>
  <how-to-verify>
    **Step 1: Access Staging Site**
    - Visit: https://staging-kinelab.fodivps2.cloud
    - Visit: https://staging-kinelab.fodivps2.cloud/admin (Payload admin)

    **Step 2: Visual Content Review**
    - [ ] All artists visible on team page
    - [ ] Each artist's portfolio items display correctly
    - [ ] Images load without broken links
    - [ ] Categories/tags applied correctly
    - [ ] Bio text and descriptions intact
    - [ ] No lorem ipsum or placeholder content

    **Step 3: Functional Testing**
    - [ ] Portfolio filtering works
    - [ ] Lightbox/modal opens for images
    - [ ] Artist detail pages load
    - [ ] Mobile responsive layout

    **Step 4: Data Quality Spot Check**
    - [ ] Pick 3 random portfolio items - verify all details match WordPress source
    - [ ] Check image quality (no degradation, focal points correct)
    - [ ] Verify dates/timestamps preserved

    **Step 5: CRITICAL - Verify Data Counts (MANDATORY)**

    Read expected counts from Plan 10-01:
    ```bash
    cat data/expected-counts.json | jq '.expectedCounts'
    ```

    Query actual counts in staging database:
    ```bash
    # Run in project directory
    npx ts-node -e "
      const payload = require('./src/payload.config');
      // Query counts
    "
    # Or query PostgreSQL directly:
    docker exec vps-panel-postgres psql -U kinelab_user -d kinelab_staging_db -c "
      SELECT 'artists' as type, COUNT(*) FROM team_members
      UNION ALL
      SELECT 'portfolio_items', COUNT(*) FROM portfolio
      UNION ALL
      SELECT 'media_files', COUNT(*) FROM portfolio_media;
    "
    ```

    **FILL IN AND VERIFY:**
    | Data Type | Expected (from 10-01) | Actual (in staging) | Match? |
    |-----------|----------------------|---------------------|--------|
    | Artists | ___ | ___ | [ ] |
    | Portfolio Items | ___ | ___ | [ ] |
    | Media Files | ___ | ___ | [ ] |
    | Categories | ___ | ___ | [ ] |

    **Step 6: Approval Decision**
    - [ ] ALL counts match exactly (no missing data)
    - [ ] Visual review passed
    - [ ] Functional tests passed
    - [ ] Artist approves migration quality

    **WARNING:** Do NOT approve if any count is LOWER than expected.
    Missing data = data loss = migration catastrophe.

    If counts don't match:
    1. Check error-log.json from import
    2. Identify which records failed
    3. Fix and re-run import
    4. Verify counts again
  </how-to-verify>
  <resume-signal>
    Type "approved" with verified counts table filled in, OR describe issues found.
    Example: "approved - artists: 5/5, portfolio: 150/150, media: 180/180"
  </resume-signal>
</task>

</tasks>

<verification>
- All WordPress content visible in staging Payload CMS
- Row counts verified: expected matches actual (zero missing records)
- Artist explicitly approved migration quality
- No broken images or missing relationships
</verification>

<success_criteria>
- Staging environment fully populated with WordPress content
- Data count verification table completed with all matches
- Artist approval obtained with explicit count confirmation
- Ready for production cutover in Plan 10-03
</success_criteria>

<output>
After completion, create `.planning/phases/10-migration-launch/10-02-SUMMARY.md`
Include the verified counts table in the summary.
</output>
