---
phase: 10-migration-launch
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - docs/dns-cutover-record.md
autonomous: false

must_haves:
  truths:
    - "DNS points to new Payload CMS site"
    - "SSL certificate active for production domain"
    - "Old WordPress site archived/disabled"
  artifacts:
    - path: "docs/dns-cutover-record.md"
      provides: "Record of DNS changes for rollback reference"
  key_links:
    - from: "DNS records"
      to: "Traefik ingress"
      via: "A record"
      pattern: "72.61.184.133"
---

<objective>
Execute DNS cutover from WordPress to Payload CMS production site.

Purpose: Complete migration by routing production domain to new site.
Output: Live production site on new infrastructure with verified DNS propagation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-migration-launch/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Current DNS State</name>
  <files>docs/dns-cutover-record.md</files>
  <action>
    Before making ANY DNS changes, document current state for rollback:

    ```bash
    # Query current DNS records
    dig kinelab.it A +short
    dig www.kinelab.it A +short
    dig kinelab.it MX +short
    dig kinelab.it TXT +short

    # Record nameservers
    dig kinelab.it NS +short
    ```

    Create docs/dns-cutover-record.md with:
    - Current A record IP (old WordPress server)
    - Current MX records (preserve for email)
    - Current TXT records (SPF, DKIM, etc.)
    - Timestamp of cutover
    - Rollback instructions

    Format:
    ```markdown
    # DNS Cutover Record - KineLab Migration

    ## Pre-Cutover State (ROLLBACK REFERENCE)
    - A record: [OLD_IP]
    - www CNAME: [value]
    - MX records: [preserved]

    ## Post-Cutover State
    - A record: 72.61.184.133
    - www CNAME: kinelab.fodivps2.cloud

    ## Rollback Procedure
    If issues arise, revert A record to [OLD_IP]
    ```
  </action>
  <verify>
    - `docs/dns-cutover-record.md` exists with pre-cutover state
    - Old IP address documented for rollback
  </verify>
  <done>Current DNS state documented for rollback reference</done>
</task>

<task type="auto">
  <name>Task 2: Update DNS Records</name>
  <files>N/A - external DNS provider</files>
  <action>
    Update DNS at provider (Hostinger or Cloudflare):

    1. Update A record for root domain:
       - Name: @ (or kinelab.it)
       - Type: A
       - Value: 72.61.184.133
       - TTL: 300 (5 minutes for quick propagation)

    2. Update www subdomain:
       - Name: www
       - Type: CNAME
       - Value: kinelab.fodivps2.cloud (or A record to same IP)
       - TTL: 300

    3. PRESERVE existing records:
       - MX records (email must continue working)
       - TXT records (SPF, DKIM, domain verification)

    Use CLI if available:
    ```bash
    # Cloudflare example (if using Cloudflare)
    ./scripts/cloudflare-setup-dns.sh kinelab.it 72.61.184.133
    ```

    Or update manually via DNS provider dashboard.
  </action>
  <verify>
    - DNS update submitted successfully
    - MX records unchanged (verify with `dig kinelab.it MX`)
    - Email delivery test passes
  </verify>
  <done>DNS records updated to point to new infrastructure</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify DNS Propagation and SSL (CRITICAL)</name>
  <what-built>
    DNS records updated to point production domain to new Payload CMS site.
    SSL certificate should be automatically provisioned by Traefik/Let's Encrypt.
  </what-built>
  <how-to-verify>
    **Step 1: Check DNS Propagation**

    Use multiple DNS checkers:
    - [ ] https://dnschecker.org - enter kinelab.it
    - [ ] `dig kinelab.it A +short` returns 72.61.184.133
    - [ ] `dig www.kinelab.it A +short` or CNAME resolves correctly

    **Step 2: Verify TTL Settings**

    Check TTL on records (important for quick rollback if needed):
    ```bash
    dig kinelab.it A | grep -A1 "ANSWER SECTION"
    # Should show TTL of 300 or similar low value
    ```

    - [ ] TTL is 300 seconds or less (allows quick rollback)
    - [ ] Document actual TTL: ___ seconds

    **Step 3: Verify SSL Certificate**

    ```bash
    # Check SSL is valid
    curl -I https://kinelab.it 2>&1 | head -20

    # Check certificate details
    echo | openssl s_client -connect kinelab.it:443 -servername kinelab.it 2>/dev/null | openssl x509 -noout -subject -dates
    ```

    - [ ] HTTPS works without certificate warnings
    - [ ] Certificate issued by Let's Encrypt
    - [ ] Certificate valid for kinelab.it (check CN/SAN)
    - [ ] Certificate not expired (check dates)

    **Step 4: Full Site Verification**

    - [ ] https://kinelab.it loads correctly
    - [ ] https://www.kinelab.it redirects or loads correctly
    - [ ] All pages accessible (home, portfolio, team, contact)
    - [ ] Images load from new infrastructure
    - [ ] Forms submit correctly
    - [ ] No mixed content warnings (HTTP resources on HTTPS page)

    **Step 5: Verify Email Still Works**

    - [ ] MX records unchanged: `dig kinelab.it MX +short`
    - [ ] Test email delivery (send test email to/from @kinelab.it)

    **Step 6: Archive Old WordPress Site**

    Once verified working:
    - [ ] Take final WordPress backup (if not already done)
    - [ ] Disable old WordPress site (maintenance mode or shutdown)
    - [ ] Keep old server available for 7 days in case rollback needed

    **Verification Summary:**
    | Check | Status |
    |-------|--------|
    | DNS propagated | [ ] |
    | TTL <= 300s | [ ] |
    | SSL valid | [ ] |
    | Site loads | [ ] |
    | Email works | [ ] |
    | Old site archived | [ ] |
  </how-to-verify>
  <resume-signal>
    Type "cutover-complete" with verification summary filled in, OR describe issues.
  </resume-signal>
</task>

</tasks>

<verification>
- DNS points to new infrastructure (72.61.184.133)
- SSL certificate valid and serving HTTPS
- TTL verified for quick rollback capability
- Production domain fully functional
- Email delivery preserved
- Old WordPress site archived
</verification>

<success_criteria>
- Production domain (kinelab.it) serves new Payload CMS site
- SSL working with valid Let's Encrypt certificate
- No broken links or mixed content
- Email continues working
- Old site safely archived
- Migration complete
</success_criteria>

<output>
After completion, create `.planning/phases/10-migration-launch/10-04-SUMMARY.md`
</output>
